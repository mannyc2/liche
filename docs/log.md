# Docs log

## 2026-05-19

- Added `docs/product-schema.md` as the next `@lili/build` target: static class authoring (`Product`, `Resource`, `Command`, `Shape`, `Field`), sibling resources/commands/bindings, normalized `Catalog` and `Capability[]`, first-class field metadata, normalized surface defaults, and a hard refactor path before OpenAPI.
- Updated build/schema/application/plan coverage docs so the next implementation sequence is Phase 3B product-schema refactor, then Phase 3C OpenAPI projection from HTTP-capable catalog entries and field metadata. OpenAPI is now documented as a filtered projection of capabilities, not a reflection of generated CLI commands or a forced CRUD/resource taxonomy.
- Expanded Phase 5 in `docs/next-plan.md` into the concrete next `@lili/releases` package slice: manifest schema, final binary byte verification, renderer registry/selection, fixture-renderer orchestration, final packed-artifact verification, and manifest-based yank dry run. Clarified that npm renderer implementation waits for Phase 6.
- Expanded `docs/tests/release-guardrails-test-plan.md` with the exact next release-package test files, fixture rules, and known-bad implementations each test should catch.
- Added `RELEASE-013` to `docs/coverage-rewrite.md` so the release package boundary is tested: `@lili/releases` consumes manifest/build-output data without runtime dependencies on `@lili/core` or `@lili/build`.
- Added Phase 7 distribution automation scope: npm, PyPI, Homebrew, and Scoop publishing must run from one release manifest plus verified artifact records, with dry-run planning, credential preflight, pre-publish hash checks, and npm platform-before-umbrella ordering. Added `RELEASE-014` through `RELEASE-017` coverage rows and later publishing test-plan entries.
- Tightened release requirements after competitive-positioning review: split renderer and publisher concerns, replaced release `schema` provenance with product/catalog provenance, added exact Bun target plus cpu-variant normalization, expanded conformance and executable metadata, added stable package artifact records, documented npm/PyPI/GitHub provenance integration, and added `RELEASE-018` through `RELEASE-021` coverage/test-plan rows.
- Added `docs/auth-session.md` as the opt-in auth/session requirement doc. Auth is catalog-owned, core exposes small runtime primitives (`SecretString`, `resolveAuth`, `resolveContext`, `SessionStore`, `applyAuth`), release manifests record non-secret auth expectations, and implementation is staged as env auth/requirements, file sessions/context, then OAuth device login. Updated product schema, canonical catalog, core boundary, transport, env, release manifest, implementation plan, and coverage with `AUTH-001` through `AUTH-012`; added `docs/tests/auth-session-test-plan.md`.
- Added Phase 3C-QA as a build mutation-testing goal: `@lili/build` should get the same package-local Stryker/Bun runner workflow as `@lili/core`, with root-catalog dependencies, implementation-module mutation scope, core-matching starting thresholds, and an initial report that drives focused follow-up tests. Updated build-system requirements, build test plan, and coverage row `BUILD-031`.
- Landed Phase 3C-QA: added `packages/build/stryker.conf.mjs`, a `mutate` script, and Stryker/Bun-runner dev deps via the root `catalog:test`. Scope mutates `catalog.ts`, `command.ts`, `digest.ts`, `field.ts`, `generate.ts`, `generate-cli.ts`, `generate-openapi.ts`, `lints.ts`, `manifest.ts`, `product.ts`, `shape.ts`, `vocabulary.ts`; excludes `src/index.ts`, `src/cli.ts`, `src/skill.ts`, `src/types.ts`, fixtures, and tests. `disableTypeChecks` is scoped to `src/**/*.ts` so the typescript-checker plugin does not inject `// @ts-nocheck` into `test/fixtures/*.generated.ts` (which would break exact-text golden comparisons). Initial baseline (1269 mutants, 127 tests, `~1m 54s` cold / `~3s` incremental): **68.62% total / 78.96% covered**, 503 killed / 134 survived / 96 no-coverage / 536 type-error-eliminated. Survivors concentrated in `lints.ts` (56%), `generate-openapi.ts` (62.5%), `manifest.ts` (63.5%), and `vocabulary.ts` (50%); `field.ts` was already 100%.
- Killed mutants on the first-landed Phase 3C-QA baseline. Added focused unit tests for `vocabulary()` merge semantics (8 cases), `canonicalize` nullish/missing-property handling (6 cases), `manifestEqualForSurface` / `hashString` (19 cases, new `test/manifest-equality.test.ts`), `lintCatalog` issue shape + ID_PATTERN + hasText + surface/openapi-on-local guards (≈25 cases). New mutation floor: **80.63% total / 88.34% covered** with `digest.ts`, `field.ts`, `manifest.ts`, `vocabulary.ts` at 100%; `lints.ts` at 97.58 (covered 100); `generate.ts` at 89.13. Ratcheted thresholds to `high: 90 / low: 85 / break: 75`. Remaining survivor concentration is `generate-cli.ts` (73.72), `generate-openapi.ts` (62.5), and `catalog.ts` (75.21) — those need fixture diversification, not more assertions, and are out of scope for the first slice.

## 2026-05-18

- Created initial rewrite docs structure.
- Added requirement links for invariant, build system, distribution, and rewrite coverage.
- Added target package layout requirements for the future Bun monorepo cutover.
- Recorded package boundary decision: required packages are `@incur/core`, `@incur/build`, and `@incur/release`; `@incur/release-extra` is conditional. Superseded below by the unified `@incur/releases` decision.
- Added application integration guidance for Vite/TanStack-style apps: define operations, implement API routes, generate CLI/surfaces, run conformance.
- Recorded execution-direction correction: `.serve()` is argv command execution, `.fetch()` is inbound HTTP handling, fetch-backed commands are in-process Request/Response bridges, and outbound remote HTTP operation transport is a separate core primitive.
- Recorded remote dispatch decision: `remote.{method,path}` in the schema means generated commands call core-owned outbound HTTP operation transport.
- Recorded OpenAPI decision: generated OpenAPI is output from canonical schema IR, not input to the MVP.
- Recorded server conformance decision: `@incur/build` owns `conform`; `generate --check` remains artifact freshness; release can consume conformance reports.
- Recorded release provenance decision: manifest must include runtime env/config expectations such as remote base URL and auth env names.
- Added focused requirement docs for HTTP transport, schema IR/OpenAPI, server conformance, npm binary packaging, and conditional release-extra renderers. Superseded below by `docs/releases.md`.
- Recorded release-extra gate decision: keep `@incur/release-extra` conditional until non-npm distribution is accepted as first-release scope. Superseded below by the single-package release renderer plan.
- Recorded transport refinement: pure request serializer first; `callHttpOperation` returns parsed output and throws structured core errors.
- Condensed docs tree: removed `concepts/`, `api/`, and `release/` pointer-file subdirectories (their content was paraphrases of root requirement files). Removed `index.md`; folded reading order into `docs/AGENTS.md`. Renamed `coverage-matrix.md` → `coverage-current.md` and `rewrite-coverage-matrix.md` → `coverage-rewrite.md` for clarity. Removed empty `sources/` placeholder directory and its associated rules (DOCS-002).
- Replaced the conditional `release-extra` package plan with a single `@incur/releases` package. Release renderers are now zero-to-all configuration choices inside that package; npm is not a special package boundary.
- Added `docs/next-plan.md` to define the monorepo cutover, build slices, remote/conformance slice, and renderer-neutral releases spine.
- Added generated-surface graph requirements so schema-driven systems can keep CLI, OpenAPI, MCP command tools, Agent Skills, docs, config schema, and later OpenAPI/product-specific adapters synchronized through explicit provenance and drift checks. Product-specific surfaces such as `wrangler.jsonc`, Workers Binding RPC metadata, dashboard metadata, SDKs, Terraform, Code Mode MCP, and generated server/API code remain adapter-gated rather than MVP behavior.
- Recorded agent consistency audit gaps: current core accepts arbitrary verbs/flags, advertises `--format`, has helper commands that ignore `--json`, lacks generated local/remote output signaling, and emits runtime-reflection OpenAPI that does not consume canonical remote bindings. Added Phase 2A and rewrite coverage rows to close these gaps.
- Clarified that canonical IR is a command/operation contract, not a product or domain model. Added `effects.kind` as the safety/lint axis, made the command manifest / `schema --json` generated surface explicit, and required fixtures that keep workflow commands first-class alongside optional CRUD/resource sugar.
- Renamed project from the upstream-derived `incur` placeholder to `lili`. Forward-facing package names are now `@lili/core`, `@lili/build`, `@lili/releases`; primary binary is `li`; build CLI binary is `li-build` (placeholder, decision deferred to Phase 3); error class `IncurError` → `LiliError`; observable strings `Incur.*Error`, `incur.cli.state`, `incur.v1`, `*.incur.*` paths, `~/.config/incur/`, `incur.local`, schema/generated file naming all renamed accordingly. Historical log entries above retain their original `@incur/*` names. Ancestry references in `AGENTS.md`, `test/parity.test.ts`, and `docs/package-layout.md` retensed and preserved. Old single-commit git history (`91b3db2 Initial imported artifact`) discarded; new history starts from this rename.
- Completed monorepo cutover (Phase 1 of `docs/next-plan.md`). Moved single-package source into `packages/core/` (history preserved via `git mv`), scaffolded `@lili/build` and `@lili/releases` as empty stubs, switched root `package.json` to Bun workspaces + catalogs. Added `packages/core/test/boundary.test.ts` proving `@lili/core` does not import `@lili/build` or `@lili/releases`. 312 tests pass.
- Removed runtime-reflection OpenAPI from `@lili/core` (`src/command/openapi.ts`, `emitOpenApi`, `ingestOpenApi`, `/openapi.json` and `/.well-known/openapi.json` fetch routes, `openapi?: Dict` field on `CommandDefinition`, and all associated tests). OpenAPI is now exclusively owned by `@lili/build` and will be generated from canonical IR + `remote.bind`, not from runtime command-tree reflection. Rationale: reflection-based OpenAPI cannot produce useful HTTP semantics (path/query/header/body placement) from argv-shaped command definitions; the agent-discovery surfaces already covered by core (MCP, `--llms`, `--json` envelopes, `--help`) are sufficient, and OpenAPI's real audience (SDK gen, Terraform providers, API gateways) needs build-time schema info core does not have.
- Built-in write-side commands (`skills add`, `mcp add`, `gen`) now honor `--json`/`--format`. When `formatExplicit` is set the output is the standard `{ ok: true, data: { path } }` envelope rendered through the active formatter; otherwise the human-friendly `wrote <path>` text is preserved for interactive terminal use. `completions` continues to emit a raw shell script regardless of `--json` (the output is literally a shell script). Closes Phase 2A item 4.
- Added `docs/core-api-boundary.md` to resolve the Phase 2 `@lili/core` public API freeze. The approved surface centers on `Cli.create().command()`, `middleware`, `z`, formatter helpers, structured errors, and public handler/types. Current state-shaped namespaces (`Parser`, `Help`, `Schema`, `Skill`, `Mcp`, `Completions`, `Fetch`, `Typegen`) must be removed, internalized, or reshaped before generated code imports core.
- Executed the Phase 2 core boundary freeze (Commit C). `packages/core/src/index.ts` now exports only the frozen surface: `Cli`, `middleware`, `z`, the `Formatter` namespace, structured error classes (`BaseError`, `LiliError`, `ParseError`, `ValidationError`), and the public types enumerated in `docs/core-api-boundary.md` (`Schema` replaces the prior `ZodSchema` alias; `CommandError`, `CreateOptions`, `FieldError`, `InferSchema`, `ResultMeta` are now exported because public types reference them). Removed from the index: `default`, `create`, `Dict`, and the `Completions`, `Errors`, `Fetch`, `Filter`, `Help`, `Mcp`, `Parser`, `Schema`-namespace, `Skill`, and `Typegen` re-exports. Stripped `Dict` from public type signatures in `packages/core/src/types.ts` (8 sites — `Cta`, `Example`, `RunContext` generics defaults, `CommandDefinition.alias`/`.optionEnv`/`.run`, `CreateOptions.config.loader`, `ServeOptions.env`, `SelectedCommand.argv.options`) in favor of explicit `Record<string, ...>` shapes; `Dict` itself remains defined for internal source use. Migrated white-box test imports off the removed index namespaces to subpath imports (`Mcp`, `Skill`, `Completions`, `Fetch`, `Parser`, `Schema`) — the boundary doc explicitly permits this for tests. Added `packages/core/test/api-snapshot.test.ts` to lock the public surface: it sorts `Object.keys` of `* as Api from '../src/index.js'` against a hard-coded list and type-imports every expected public type so `tsc --noEmit` fails if a type drops from the index. 291 tests pass; typecheck clean.
- Closed three audit findings on the Phase 2 freeze: (1) outbound HTTP transport primitives (`serializeHttpOperationRequest`, `callHttpOperation`) were listed in Phase 2 but do not exist in code — moved them out of the Phase 2 freeze list and verification in `docs/next-plan.md` with an explicit note that they land and freeze in Phase 4 (remote and conformance slice); (2) added a package-consumer boundary test in `@lili/build` (`packages/build/test/core-consumer-boundary.test.ts`) that imports `@lili/core` by package name (not relative source path) and asserts the resolved value exports equal the frozen surface — complements the existing source-level snapshot test; (3) deleted the dead `export default middleware` from `packages/core/src/cli/context.ts` (no consumers in source or tests).
- Phase 3 Commit 1 — `@lili/build` schema API and canonical IR landed. `defineProgram`, `operation`, `vocabulary`, `DEFAULT_GENERATED_VOCABULARY` build a `RuntimeNormalizedProgram`; `normalizeProgram` projects it to `CanonicalProgramIR` (per `docs/schema-ir-openapi.md`). Default vocabulary: verbs `get/list/create/update/delete/run`; control flags `json/local/remote/force`; forbidden verb `info`; forbidden flags `format`, `skip-confirmations`, `skipConfirmations`. Per-program `vocabulary({...})` overrides merge with defaults (unique). `policy` derives from `effects` when unset (`delete`/dangerous → destructive + requires-confirmation; `read`/`delete` → idempotent). `remote.bind` defaults `body: false`, empty `path`/`query`/`headers`. Input/output Zod schemas project via `z.toJSONSchema` and the JSON Schema `required` array is sorted so declaration-order changes do not perturb the digest. `canonicalDigest` sorts object keys recursively, normalizes undefined away, refuses functions, and returns `sha256:<hex>`. 12 build tests cover IR shape, policy derivation, bind defaults, JSON Schema projection, formatted-equivalent-program digest equality (Phase 3 verification line), and reordered-input-property digest equality. Workspace: 291 core + 12 build tests pass; all three packages typecheck clean.
- Phase 3 Commit 2 — vocabulary and structural lints landed in `@lili/build`. `lintProgram(runtime)` runs `vocabulary/verb`, `vocabulary/forbidden`, `override/guarded`, `operation/id-stable`, `operation/output-required`, and `operation/locality-required`. `vocabulary/flag` is scoped to declared control flags only — operation input fields (`limit`, `projectId`, `includeDeployments`, etc.) are intentionally not in scope and a regression test guards that. Forbidden-name recommendations: `info → get`, `format → json`, `skip-confirmations`/`skipConfirmations → force`. Operation ids must match `^[a-z][a-zA-Z0-9]*(?:\.[a-z][a-zA-Z0-9]*)*$` (lowerCamel dot-segments). Void/never/undefined output schemas fail `operation/output-required`. Locality default must be in modes and modes must be non-empty. Remote/portability/example/schema lints deferred to Phase 4. 11 new lint tests; 23 build + 291 core tests pass.
- Phase 3 Commit 3 — Core Envelope Re-Freeze. Three deliberate widenings to `@lili/core` to support generated CLIs: (1) `ResultMeta` widened from `{ cta?: CtaBlock }` to `Record<string, unknown> & { cta?: CtaBlock }`, and `ctx.ok` now preserves all meta keys to the result envelope; (2) `CreateOptions.generated?: { machineOutput: 'envelope'; disabledGlobals?: readonly DisabledGlobal[] }` opts a CLI into full `{ok, data, meta}` output under `--json` and into global-flag rejection; (3) new public type `DisabledGlobal` (currently `'format'`). Handwritten CLIs are unchanged — without `generated`, `--json` still emits bare data and `--format` still works. Updated `docs/core-api-boundary.md` with the re-freeze block; `api-snapshot.test.ts` and `core-consumer-boundary.test.ts` extended with `DisabledGlobal`. Out of scope: `ctx.sources.options` and `meta.locality.source === 'config'` — both require core option-provenance and will land in a separate re-freeze. New `test/envelope-mode.test.ts` (5 tests) covers envelope output, `--format` rejection, handwritten compat, and arbitrary-meta round-trip. Workspace: 296 core + 23 build tests pass.
- Phase 3 Commit 4 — first generated CLI vertical slice. `generateCli(ir, options)` in `@lili/build` renders a deterministic TypeScript file from `ProgramIR` and `GenerateOptions`. Header carries generator version, schema name/version, canonical IR digest, generation-options digest, surface id (`cli`), and source (`canonical-ir`). Imports are limited to `Cli, z` from `@lili/core` plus local impl modules (`.ts` → `.js` rewrite). Operations group by first command segment; each command emits `options`/`output` Zod schemas derived from the IR JSON Schema projection (supports `z.object`, `z.string/number/boolean/array/enum`, `.optional()`, `.default(value)`). Each generated `run` calls the local impl with `ctx.options` and returns `ctx.ok(data, { locality: { mode: <schema-default>, source: 'schema-default' } })`. Root program emits `generated: { machineOutput: 'envelope', disabledGlobals: ['format'] }`. Fixture: `acme.program.ts` with `projects.get` (CRUD-like) and `projects.deploy` (workflow, `effects.kind: 'exec'`, no `remote.bind`). Golden `acme.generated.ts` is byte-locked against the generator output. Parity test runs both the generated and handwritten CLIs (`acme.handwritten.ts`) with `--projectId abc --json` and `--projectId abc --target prod --json` and asserts identical envelopes (Phase 3 verification: same JSON for same input). Boundary discipline test asserts the generated source imports exactly `Cli, z` from `@lili/core` and does not reach into subpaths or internals. `--format json` is rejected on the generated CLI. 30 build + 296 core tests pass.
- Phase 3 Commit 6 — provenance, surface manifest, `li-build generate --check`. `@lili/build` now has a top-level `generateToDir` / `checkAgainstDir` plus a Bun-script `li-build` binary at `packages/build/src/cli.ts` (registered in `package.json#bin`). `generate <program.ts>` writes `lili.generated.ts` and `lili.generated.manifest.json` next to the program (or under `--out <dir>`); `generate --check` re-derives both and reports drift. Manifest matches the `GeneratedSurfaceManifest` shape from `docs/schema-ir-openapi.md`: `manifestVersion: 1`, `schema: { name, version, digest }`, `generatorVersion`, and a single `surfaces[]` entry with `id: 'cli'`, `source: 'canonical-ir'`, `inputDigest`, `generationOptionsDigest`, `outputDigest`, and relative `artifacts: ['lili.generated.ts']`. Drift detection covers all four levels: hand-edited generated file → `surface 'cli' output digest mismatch`; hand-edited manifest digests → `inputDigest`/`generationOptionsDigest`/`outputDigest` mismatch; missing artifact or manifest file → clear "missing" message. Phase 3A verification line satisfied: changing `surfaceId` changes `generationOptionsDigest` and `outputDigest` without changing `inputDigest`. End-to-end smoke (manual): `bun packages/build/src/cli.ts generate ... && --check` round-trips and a `sed` edit fails check with exit 1. 42 build + 296 core tests; all checks clean. **Phase 3 vertical slice complete.**
- Phase 3 Commit 5 — locality resolution in generated CLIs. Multimode operations (`locality.modes.length > 1`) get `--local` and `--remote` injected into the options schema by the generator; single-mode operations do not (help reflects this). Resolution order in generated `run`: explicit flag → schema default. `meta.locality.source` is `"flag"` (explicit) or `"schema-default"` (default). `--local --remote` produces `ctx.error({ code: 'LOCALITY_CONFLICT' })` before the local impl runs. `--remote` without an `op.remote` HTTP binding produces `ctx.error({ code: 'REMOTE_NOT_IMPLEMENTED' })` — Phase 3 carries the locality mechanics but does not fake remote transport (that's Phase 4). Control flags are destructured out of `ctx.options` (`const { local: _local, remote: _remote, ...input }`) so they never leak into the local implementation's input. Fixture `projects.get` updated to `locality.modes: ['local', 'remote']` to exercise both branches; `projects.deploy` stays single-mode. Golden `acme.generated.ts` re-locked. `meta.locality.source === 'config'` remains a documented known gap pending core option-provenance. New `test/locality.test.ts` covers schema-default, flag override, conflict rejection, remote-not-implemented, and the help-advertisement asymmetry between single-mode and multimode ops. 36 build + 296 core tests pass.
- Updated the vocabulary and build-CLI direction after review. Vocabulary is now a positive allowlist only: `vocabulary({...})` extends defaults, while an explicit `Vocabulary` object can replace them; forbidden verb/flag and guarded-override requirements were removed. Core helper built-ins are opt-in via `CreateOptions.builtins`; `completions` remains on by default, while `gen`, `mcp`, and `skills` default off. Rewrote `packages/build/src/cli.ts` to use `@lili/core` directly instead of a private argv parser; full self-hosted generation remains deferred until the generator contract stabilizes.
- Reframed `@lili/build` as a contract compiler rather than a CLI-program DSL. The public authoring API is now `Contract.create(...).operation(...)`; `defineProgram()` and the standalone `operation()` helper were removed. Operations no longer author a parallel `verb` field — canonical IR derives `verb` from the final command segment, and `lintContract()` checks that derived action against the active vocabulary. Renamed runtime/canonical build types and entry points from program-shaped names to contract-shaped names (`normalizeContract`, `ContractIR`, `ContractRemoteIR`) and re-locked the generated CLI fixture from `acme.contract.ts`. Updated build/schema/application docs and coverage wording to match the contract model. `bun run check` and `bun run test` pass locally after the cutover.
- Added packaged skill support to core and enabled build-tool agent helpers. `CreateOptions.skill` now lets a CLI provide authored skill markdown/index; reflection-based skill generation remains the fallback. `li-build` opts into `skills` and `mcp` built-ins, ships authored `li-build` skill content covering contracts, vocabulary, generation, and drift checks, and `li-build skills add` installs that content. Generated product CLIs still leave agent helper built-ins disabled by default so IR-generated skill/MCP surfaces remain canonical. Local verification: `bun run --filter @lili/core check`, `bun run --filter @lili/core test`, `bun run --filter @lili/build check`, and `bun run --filter @lili/build test`.
- Tightened the API-surface review fixes before dogfooding. `SkillDefinition.markdown` is optional to match the existing reflection fallback. `ContractRemote.baseUrl` now requires either `envVar` or `literal` at the type level, with `lintContract()` also emitting `contract/remote-base-url` for malformed runtime values. `lintContract()` now emits `operation/locality-binding` when an operation declares `local` or `remote` locality without the matching execution binding. `Contract.kind` remains documented as the runtime loading tag for `li-build` module imports.

- Phase 3D-A landed (env-only auth and capability requirements, per `docs/next-plan.md` Phase 3D-A and `docs/auth-session.md` slice A). `@lili/core` gains `secret(value)` + `SecretString`, env-driven `resolveAuth`/`resolveContext`/`applyAuth`/`authMetaFromCredential`, and structured `AUTH_*` errors built on `LiliError` with a new `details: Record<string, unknown>` slot (`CommandError` envelope mirrors it). `@lili/build` gains the `Auth` namespace (`Auth.none|bearer|apiKey`, `Auth.token.env`, `Auth.context.env|remote`), `Product.auth(...)` and `Product.context(...)` chain methods (single provider, unique-id contexts). The legacy single `permission?: string` slot on capabilities is hard-cut to `requires: { auth, contexts, permissions }`; `normalizeProduct` now requires an explicit auth posture and validates `requires.contexts` against declared contexts and `requires.auth` against the chosen provider. `GeneratedSurfaceManifest` carries a `auth.providers[]` block (id/kind/modes/envVars/contexts/requiredRuntimeCapabilities). Generated CLI emits `AUTH_PROVIDER` + `CONTEXTS` constants, injects context flags as required `z.string()` options, and runs a `resolveAuth`/`resolveContext`/`applyAuth` preamble before the Phase-4 transport stub when any capability declares `requires.auth` or `requires.contexts`; products with `Auth.none()` and no contexts produce byte-equivalent generator output to Phase 3C. Tests: `packages/core/test/auth/{secret,errors,resolve}.test.ts` (env precedence, CI vs CLI invocation, custom headers, redaction, structured error details); `packages/build/test/catalog-normalization.test.ts` (Auth/Context/Capability requires + Surface manifest describe blocks); `packages/build/test/fixtures/workers-auth.product.ts` + `packages/build/test/generate-cli-auth.test.ts` (generated source assertions plus runtime execution: missing env → `AUTH_MISSING`, env set → resolution succeeds and Phase-4 stub fires; raw token never leaks). Workers fixture rewired to `.auth(Auth.none())` and `requires.permissions: [...]`; vocabulary-lints helper auto-applies `Auth.none()` to stay focused on the lint rule. Generated golden fixtures regenerated to track the catalog-digest change. 555 tests across 35 files; all packages typecheck clean.
